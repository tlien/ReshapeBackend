apiVersion: v1
kind: Service
metadata:
  name: {{ template "templateservice.fullname" . }}
  labels:
    app: {{ template "templateservice.name" . }}
    chart: {{ template "templateservice.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "templateservice.name" . }}
    release: {{ .Release.Name }}
